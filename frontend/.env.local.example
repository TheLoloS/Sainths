# Konfiguracja środowiska dla Sainths Frontend
# Skopiuj ten plik do .env.local i dostosuj wartości

# URL backendu Socket.io
# Dla pracy lokalnej (localhost):
# NEXT_PUBLIC_BACKEND_URL=http://localhost:3001

# Dla dostępu z sieci lokalnej (zastąp IP swoim adresem):
# NEXT_PUBLIC_BACKEND_URL=http://192.168.0.100:3001

# Uwaga: Frontend automatycznie wykryje IP serwera jeśli ta zmienna nie jest ustawiona
# Pod warunkiem że frontend i backend są na tym samym hoście
